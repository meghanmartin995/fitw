function addModal(innerHTML) {
  const modal = document.querySelector('#exampleModal')
  modal.innerHTML = innerHTML
}

addModal("<%= escape_javascript(render :partial => '/posts/modal', :locals => {post: @post) %>")
